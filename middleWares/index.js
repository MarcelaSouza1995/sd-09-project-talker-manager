const validateLogin = require('./validateLogin');
const validateToken = require('./validateToken');
const validateNameAge = require('./validateNameAge');
const validateTalk = require('./validateTalk');
const regexCheckTalk = require('./regexCheckTalk');

module.exports = {
  validateLogin,
  validateToken,
  validateNameAge,
  validateTalk,
  regexCheckTalk,
};
