const errorTreatment = require('./errorTreatment');
const readFile = require('./readFile');
const authentication = require('./authetication');
const writeFile = require('./writeFile');

module.exports = {
  errorTreatment,
  readFile,
  authentication,
  writeFile,
};
